Supplementary S0-S4 (auto-generated 2026-08-11 17:20:23; KITTI 00 batch
updated manually 2026-08-13 to the three-run batch)

Generated by: bash make_supplementary.sh (run on the experiment machine)

Files:
  S1_per_run_ATE.csv      per-run ATE RMSE; TUM in cm (x100), KITTI in m.
  S2_adoption.csv         per-run LK adoption from 'LK pose used' statistics.
  S3_frame_events.csv     per-run trajectory/GT line counts and event counts.
  S3_failure_excerpts.txt log excerpts for the reported failure modes.
  S0_kitti00.csv          KITTI 00 three-run batch (per-run ATE RMSE, m;
                          n = 3 per method, tau = 0.40).
  S4_lk.patch             unified diff, baseline -> LK-only implementation.
  S4_adaptive_gate.patch  unified diff, LK-only -> adaptive-gate implementation.

S1 columns: domain, group, sequence, method, threshold, run, ate, unit,
            lk_used_frames, lk_used_pct, avg_tracked_points
S2 columns: domain, group, sequence, method, threshold, run, lk_used_frames,
            total_frames, adoption_pct, source (log / lkstats / missing)
S3 columns: domain, group, sequence, method, threshold, run, traj_lines,
            gt_lines, newmap_events, relocalizations, lost_events,
            gate_alarms, segfault

Notes:
  - ate is ATE RMSE from evo (SE(3) Umeyama alignment), rmse line.
  - fr1_desk appears in group 'fr12' (n=3) and group 'fr1desk_ext' (n=12);
    Table 1 of the manuscript uses the fr1desk_ext batch.
  - fr3 tau=0.40 cells pool groups 'fr3_ablation' and 'fr3_repeat' (n=6).
  - newmap_events = 'Creation of new map with id:' occurrences minus 1
    (the initial map).
  - gate_alarms = number of '[LK-ALARM' occurrences in the run log.
  - segfault = 'Y' when 'Segmentation fault' appears in the run log;
    trajectory completeness is given by traj_lines vs gt_lines.
  - KITTI 00: three-run batch (n = 3 per method, tau = 0.40); ATE read from
    kitti00_{baseline,lk}_r1-3_evo_summary.txt; LK adoption stats from
    kitti00_lk_r1-3_stats.txt (source 'lkstats' in S2).
  - avg_tracked_points is parsed from the LK statistics block; it is empty
    when the log/lkstats format differs.
  - run is the evo file suffix number; for kitti05_ablation, one legacy
    pre-numbering run (file without an _rN suffix) is labeled run=0, so a
    cell with n=4 contains three numbered runs plus this legacy run.

Row counts: S1=152, S2=152, S3=152; S4 patches: lk=ok (5 files), adaptive_gate=ok (1 files)

Groups: fr12 (fr1/fr2 n=3 batch), fr1desk_ext (fr1_desk n=12 batch),
  fr3_ablation (threshold ablation), fr3_baseline (fr3 n=3 baselines),
  fr3_ablation_discarded / fr3_baseline_discarded (legacy single-run ablation/
  baseline files without an _rN suffix; NOT included in S1-S3 or in any table
  cell, kept only as an audit trail in the console group-means output),
  fr3_repeat (fr3 tau=0.40 repeat batch; pooled with fr3_ablation tau=0.40
  in the manuscript tables), legacy_single_run (earlier single-run baselines,
  source of the corrected 31%/64% claims; not used in Tables 1-4),
  a1_*/a2_*/a3_* (adaptive-gate experiment), kitti05_ablation,
  kitti05_baseline,
  kitti00_batch (sequence 00 three-run batch, n = 3 per method).

Duplicate files (same name and content, found in multiple
  directories) are counted once; skipped paths are listed in
  the console output.
